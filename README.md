# WhatsApp Food Ordering System (Backend)

## Setup
```bash
git clone <your-repo>
cd whatsapp-food-backend
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
